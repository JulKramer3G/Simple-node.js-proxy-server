# Simple node.js proxy server (CORS ENABLED)
Simple node.js proxy server to make http requests CORS-compilant

# Usage
Run ```yarn add request express``` to install dependencies.
Run ```yarn start``` to start the proxy.
